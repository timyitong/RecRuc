<%= form_for(@user_curlocation) do |f| %>
  <% if @user_curlocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_curlocation.errors.count, "error") %> prohibited this user_curlocation from being saved:</h2>

      <ul>
      <% @user_curlocation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :location_id %><br />
    <%= f.number_field :location_id %>
  </div>
  <div class="field">
    <%= f.label :open_time %><br />
    <%= f.date_select :open_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
