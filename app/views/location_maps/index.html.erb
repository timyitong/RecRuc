<h1>Listing location_maps</h1>

<table>
  <tr>
    <th>Location</th>
    <th>Mapurl</th>
    <th>Valid</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @location_maps.each do |location_map| %>
  <tr>
    <td><%= location_map.location_id %></td>
    <td><%= location_map.inuse %></td>
    <td><%= location_map.map_file_name %></td>
    <td><%= location_map.location_id%></td>
    <td><%= link_to 'Show', location_map %></td>
    <td><%= link_to 'Edit', edit_location_map_path(location_map) %></td>
    <td><%= link_to 'Destroy', location_map, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Location map', new_location_map_path %>
