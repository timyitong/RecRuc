<%= form_for(@location_map) do |f| %>
  <% if @location_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_map.errors.count, "error") %> prohibited this location_map from being saved:</h2>

      <ul>
      <% @location_map.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.file_field :map %>
  </div>
  <div class="field">
    <%= f.number_field :location_id %>
  </div>
  <div class="actions">
    <%= f.submit 'Update Picture'%>
  </div>
<% end %>
