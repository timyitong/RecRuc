<h1>Listing user_group_follows</h1>

<table>
  <tr>
    <th>User group</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_group_follows.each do |user_group_follow| %>
  <tr>
    <td><%= user_group_follow.user_group_id %></td>
    <td><%= user_group_follow.user_id %></td>
    <td><%= link_to 'Show', user_group_follow %></td>
    <td><%= link_to 'Edit', edit_user_group_follow_path(user_group_follow) %></td>
    <td><%= link_to 'Destroy', user_group_follow, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User group follow', new_user_group_follow_path %>
