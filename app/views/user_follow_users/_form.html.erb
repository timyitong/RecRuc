<%= form_for(@user_follow_user) do |f| %>
  <% if @user_follow_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_follow_user.errors.count, "error") %> prohibited this user_follow_user from being saved:</h2>

      <ul>
      <% @user_follow_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :follow_id %><br />
    <%= f.number_field :follow_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
